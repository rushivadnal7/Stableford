// Placeholder. Phase 1 is the backend (database, domain logic, API); the UI comes in phase 2.
export default function HomePage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Stableford</h1>
      <p>API is live under <code>/api</code>. The interface is built in phase 2.</p>
    </main>
  );
}
